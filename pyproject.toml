[tool.poetry]
name = "rag-mcp-server"
version = "0.1.0"
description = "RAG MCP Server scaffold"

[tool.poetry.dependencies]
python = "^3.11"
fastmcp = "^0.1.0"
pydantic = "^2.5.0"
pydantic-settings = "^2.1.0"
logfire = "^0.20.0"
asyncio = "^3.4.3"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q"
